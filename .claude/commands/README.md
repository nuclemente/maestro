# Slash-commands utilitários

Esta pasta é reservada para slash-commands de **dev** que **não** são skills (ex.: helpers locais, atalhos de build). Comece vazia.

Para criar uma feature funcional invocável por slash-command, use `.claude/skills/<nome>/` — ver `.claude/skills/README.md`.
